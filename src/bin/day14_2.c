int main(int argc, char argv[][]) {
    unsigned result = 0;
    
    
    // let (mut rs, mut cs) = (rows, cols);
    // let step = 0;
    
    // let grid = board(rows, cols, &pcounts, &positions);
    // println!("{:#?}", grid);
    
    printf("%u", result);
}
